syntaxjs/test/direct directory
-----------------


Tests which are run directly by calling the interpreter and passing the
code as argument to the interpreter

    These files are written to be run with

	node syntax.js <filename>