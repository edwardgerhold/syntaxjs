
var syntaxjs = require("syntaxjs");

// before moving into syntax0.js

// figure out some ast-query-selector (it´s a tree like any other tree and can be queried)

function querySelector(node, query) {
        
    // querySelector(node, "CallExpression") returns all CallExpressions from the node on

    // querySelector(node, ".id") returns all nodes with an .id property from the node on

}

